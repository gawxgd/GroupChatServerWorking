# Server Application
The server application manages connected users, provides an admin interface to browse users, send broadcast messages, and disconnect users. It also logs all messages for record-keeping.
